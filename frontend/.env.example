# Local development uses the Vite proxy by default.
# For Vercel, set this in Project Settings > Environment Variables.
# Include the API version path and do not add a trailing slash.
VITE_API_BASE_URL=https://eventforge-api.onrender.com/api/v1
